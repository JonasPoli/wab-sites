{% extends 'admin/wab_base.html.twig' %}{% import 'admin/page/_form.html.twig' as f %}
{% block title %}Editar Página{% endblock %}{% block page_title %}Editar Página{% endblock %}
{% block body %}<div class="ph"><h1>{{ page.title }}</h1><a href="{{ path('admin_page_index') }}" class="btn btn-g"><sl-icon name="arrow-left"></sl-icon> Voltar</a></div>
<div class="card">{{ f.page_form(page, path('admin_page_edit', {id: page.id}), categories) }}</div>{% endblock %}
