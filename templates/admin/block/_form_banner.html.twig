<form method="post" action="{{ action }}" class="af" enctype="multipart/form-data">
    <input type="hidden" name="type" value="banner">

    {# ── Imagem de fundo ── #}
    <div class="af-field">
        <label>Imagem de fundo</label>
        <div style="display:flex;align-items:center;gap:1.5rem;flex-wrap:wrap">
            {% if block.image %}
                <div>
                    <img id="banner-preview"
                         src="{{ asset('uploads/page_block/' ~ block.image) }}"
                         alt="Preview atual"
                         style="height:80px;width:160px;object-fit:cover;border-radius:8px;border:1px solid var(--border)">
                    <div style="font-size:.7rem;color:var(--muted);margin-top:.3rem;text-align:center">Atual</div>
                </div>
            {% else %}
                <div id="banner-preview"
                     style="width:160px;height:80px;border-radius:8px;border:1px dashed var(--border);display:flex;align-items:center;justify-content:center;color:var(--muted)">
                    <sl-icon name="image" style="font-size:1.8rem"></sl-icon>
                </div>
            {% endif %}
            <div style="flex:1">
                <label for="banner-upload" style="display:inline-flex;align-items:center;gap:.5rem;padding:.5rem 1.1rem;border-radius:8px;border:1px dashed var(--primary);color:var(--primary);font-size:.85rem;font-weight:600;cursor:pointer;transition:all .2s ease">
                    <sl-icon name="upload"></sl-icon> Selecionar imagem
                </label>
                <input type="file" id="banner-upload" name="imageFile"
                       accept="image/webp,image/jpeg,image/png"
                       style="display:none"
                       onchange="previewBanner(this)">
                <p class="af-hint" style="margin-top:.4rem">WebP, JPG ou PNG · Recomendado: 1920×600 px · max 5 MB<br>
                Será exibida com overlay da cor primária do tenant (15% opacidade)</p>
            </div>
        </div>
    </div>

    <div class="af-field">
        <label>Título <span class="req">*</span></label>
        <input type="text" name="title" class="ai" value="{{ block.title }}" required placeholder="Título principal do banner">
    </div>
    <div class="af-field">
        <label>Subtítulo</label>
        <input type="text" name="text" class="ai" value="{{ block.text }}" placeholder="Subtítulo ou descrição curta">
    </div>
    <div class="af-row">
        <div class="af-field">
            <label>Texto do botão (CTA)</label>
            <input type="text" name="config[ctaText]" class="ai" value="{{ block.config.ctaText ?? '' }}" placeholder="Ex: Saiba mais">
        </div>
        <div class="af-field">
            <label>Link do botão</label>
            <input type="text" name="config[ctaLink]" class="ai" value="{{ block.config.ctaLink ?? '' }}" placeholder="Ex: /p/servicos ou #contato">
        </div>
    </div>
    
    <div class="af-field" style="flex-direction:row;align-items:center;gap:.75rem">
        <input type="hidden" name="config[active]" value="0">
        <sl-switch name="config[active]" value="1" {{ (block.config.active ?? '1') == '1' ? 'checked' : '' }}></sl-switch>
        <label style="color:var(--text)">Banner ativo</label>
    </div>

    <div class="af-actions">
        <button type="submit" class="btn btn-p"><sl-icon name="check-lg"></sl-icon> Salvar</button>
        <a href="{{ path('admin_block_index', {sectionId: block.section.id ?? section.id}) }}" class="btn btn-g">Cancelar</a>
    </div>
</form>

<script>
function previewBanner(input) {
    if (!input.files || !input.files[0]) return;
    const file = input.files[0];
    if (file.size > 5 * 1024 * 1024) {
        alert('Arquivo muito grande. Máximo 5 MB.');
        input.value = '';
        return;
    }
    const reader = new FileReader();
    reader.onload = e => {
        let preview = document.getElementById('banner-preview');
        if (preview.tagName === 'DIV') {
            const img = document.createElement('img');
            img.id = 'banner-preview';
            img.style.cssText = 'height:80px;width:160px;object-fit:cover;border-radius:8px;border:1px solid var(--border)';
            img.alt = 'Preview';
            preview.replaceWith(img);
            preview = img;
        }
        preview.src = e.target.result;
    };
    reader.readAsDataURL(file);
}
</script>
