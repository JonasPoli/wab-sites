<!DOCTYPE html>
<html lang="pt-BR" class="sl-theme-dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Admin{% endblock %} — WAB Sites</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@shoelace-style/shoelace@2.19.1/cdn/themes/dark.css">
    <script type="module" src="https://cdn.jsdelivr.net/npm/@shoelace-style/shoelace@2.19.1/cdn/shoelace-autoloader.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    {# Font Awesome Free — usado no icon picker e nos blocos #}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" crossorigin="anonymous" referrerpolicy="no-referrer">
    <style>
    *,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
    :root{
        --sw:260px;--hh:60px;
        --bg:#0d0f1a;--bg2:#131625;--bg3:#1a1e35;
        --border:rgba(255,255,255,.07);
        --text:#e2e8f0;--muted:rgba(255,255,255,.45);
        --primary:#4f6ef7;--secondary:#f7c34f;
        --success:#22c55e;--warning:#f59e0b;--danger:#ef4444;
        --radius:10px;--ease:.2s ease;
    }
    body{font-family:'Inter',system-ui,sans-serif;background:var(--bg);color:var(--text);display:flex;min-height:100vh;font-size:.9rem;line-height:1.6}
    a{color:inherit;text-decoration:none}
    /* Sidebar */
    .sb{width:var(--sw);background:var(--bg2);border-right:1px solid var(--border);display:flex;flex-direction:column;position:fixed;top:0;left:0;bottom:0;z-index:50;overflow-y:auto}
    .sb-logo{padding:1.25rem 1.5rem;border-bottom:1px solid var(--border);display:flex;align-items:center;gap:.75rem;font-size:1rem;font-weight:800;color:var(--text)}
    .logo-dot{width:28px;height:28px;border-radius:8px;background:linear-gradient(135deg,var(--primary),var(--secondary));display:flex;align-items:center;justify-content:center;font-size:.8rem;font-weight:900;color:#fff;flex-shrink:0}
    .nav{padding:1rem 0;flex:1}
    .nav-sec{padding:.5rem 1.5rem;font-size:.7rem;font-weight:700;text-transform:uppercase;letter-spacing:.08em;color:var(--muted);margin-top:.75rem}
    .nl{display:flex;align-items:center;gap:.75rem;padding:.6rem 1.5rem;color:var(--muted);font-size:.875rem;font-weight:500;border-left:3px solid transparent;transition:all var(--ease)}
    .nl:hover{background:rgba(255,255,255,.04);color:var(--text);border-left-color:var(--primary)}
    .nl.on{background:rgba(79,110,247,.1);color:var(--primary);border-left-color:var(--primary);font-weight:600}
    .nl sl-icon{font-size:1.1rem}
    .nb{margin-left:auto;min-width:20px;height:20px;padding:0 6px;border-radius:50px;background:var(--danger);color:#fff;font-size:.7rem;font-weight:700;display:flex;align-items:center;justify-content:center}
    /* User footer */
    .sb-user{padding:1rem 1.5rem;border-top:1px solid var(--border);display:flex;align-items:center;gap:.75rem}
    .av{width:34px;height:34px;border-radius:50%;background:linear-gradient(135deg,var(--primary),var(--secondary));display:flex;align-items:center;justify-content:center;font-size:.85rem;font-weight:700;color:#fff;flex-shrink:0}
    .av-name{font-size:.82rem;font-weight:600}
    .av-role{font-size:.73rem;color:var(--muted)}
    .av-logout{margin-left:auto;color:var(--muted);transition:color var(--ease)}
    .av-logout:hover{color:var(--danger)}
    /* Main */
    .main{margin-left:var(--sw);flex:1;display:flex;flex-direction:column;min-height:100vh}
    /* Topbar */
    .tb{height:var(--hh);background:var(--bg2);border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;padding:0 1.75rem;position:sticky;top:0;z-index:40}
    .tb-title{font-size:1rem;font-weight:700}
    /* Content */
    .cnt{padding:2rem 1.75rem;flex:1}
    /* Impersonation */
    .imp-bar{background:linear-gradient(90deg,#7c3aed,#4f6ef7);color:#fff;text-align:center;padding:.6rem;font-size:.82rem;font-weight:600}
    .imp-bar a{color:#fff;text-decoration:underline;margin-left:.5rem}
    /* Page header */
    .ph{display:flex;align-items:center;justify-content:space-between;margin-bottom:1.75rem;gap:1rem}
    .ph h1{font-size:1.4rem;font-weight:800}
    /* Card */
    .card{background:var(--bg2);border:1px solid var(--border);border-radius:var(--radius);padding:1.5rem}
    .card+.card{margin-top:1.25rem}
    /* Buttons */
    .btn{display:inline-flex;align-items:center;gap:.5rem;padding:.55rem 1.25rem;border-radius:8px;font-size:.85rem;font-weight:600;cursor:pointer;border:none;transition:all var(--ease);font-family:inherit}
    .btn-p{background:var(--primary);color:#fff}
    .btn-p:hover{background:color-mix(in srgb,var(--primary) 85%,#fff)}
    .btn-s{background:var(--success);color:#fff}
    .btn-s:hover{opacity:.88}
    .btn-d{background:var(--danger);color:#fff}
    .btn-d:hover{opacity:.88}
    .btn-g{background:rgba(255,255,255,.06);color:var(--text);border:1px solid var(--border)}
    .btn-g:hover{background:rgba(255,255,255,.1)}
    .btn-sm{padding:.35rem .85rem;font-size:.8rem}
    /* Table */
    .tw{overflow-x:auto}
    .at{width:100%;border-collapse:collapse}
    .at thead{background:var(--bg3)}
    .at th{padding:.75rem 1rem;text-align:left;font-size:.75rem;font-weight:700;text-transform:uppercase;letter-spacing:.05em;color:var(--muted);border-bottom:1px solid var(--border);white-space:nowrap}
    .at td{padding:.75rem 1rem;border-bottom:1px solid rgba(255,255,255,.04);vertical-align:middle}
    .at tr:last-child td{border-bottom:none}
    .at tr:hover td{background:rgba(255,255,255,.02)}
    .ta{display:flex;gap:.5rem}
    /* Form */
    .af{display:flex;flex-direction:column;gap:1.25rem}
    .af-row{display:grid;grid-template-columns:1fr 1fr;gap:1.25rem}
    .af-field{display:flex;flex-direction:column;gap:.4rem}
    .af-field label{font-size:.8rem;font-weight:600;color:var(--muted)}
    .af-field label .req{color:var(--danger)}
    .ai,.as,.ate{width:100%;padding:.65rem 1rem;border-radius:8px;background:var(--bg3);border:1px solid var(--border);color:var(--text);font-size:.88rem;font-family:inherit;outline:none;transition:border-color var(--ease)}
    .ai:focus,.as:focus,.ate:focus{border-color:var(--primary)}
    .ai::placeholder,.ate::placeholder{color:var(--muted)}
    .ate{resize:vertical;min-height:120px}
    .as option{background:var(--bg2)}
    .af-hint{font-size:.75rem;color:var(--muted)}
    .af-group{display:flex;flex-direction:column;gap:1.25rem}
    .af-group--inline{flex-direction:row;flex-wrap:wrap;gap:1.5rem;padding:.75rem 1rem;background:var(--bg3);border:1px solid var(--border);border-radius:8px}
    .af-check{display:flex;align-items:center;gap:.65rem;cursor:pointer;font-size:.88rem;color:var(--text);user-select:none}
    .af-check input[type=checkbox]{width:16px;height:16px;accent-color:var(--primary);cursor:pointer;flex-shrink:0}
    .af-actions{display:flex;gap:.75rem;align-items:center;padding-top:.5rem;border-top:1px solid var(--border)}
    /* Badges */
    .badge{display:inline-flex;align-items:center;padding:.2rem .65rem;border-radius:50px;font-size:.72rem;font-weight:700}
    .bg-ok{background:rgba(34,197,94,.15);color:#4ade80}
    .bg-warn{background:rgba(245,158,11,.15);color:#fbbf24}
    .bg-neu{background:rgba(255,255,255,.08);color:var(--muted)}
    .bg-inf{background:rgba(79,110,247,.15);color:#818cf8}
    .bg-err{background:rgba(239,68,68,.15);color:#f87171}
    /* Alert */
    .al{padding:.9rem 1.25rem;border-radius:var(--radius);margin-bottom:1.25rem;font-size:.88rem;font-weight:500;display:flex;align-items:center;gap:.75rem}
    .al-ok{background:rgba(34,197,94,.1);border:1px solid rgba(34,197,94,.2);color:#4ade80}
    .al-warn{background:rgba(245,158,11,.1);border:1px solid rgba(245,158,11,.2);color:#fbbf24}
    .al-err{background:rgba(239,68,68,.1);border:1px solid rgba(239,68,68,.2);color:#f87171}
    /* Stats */
    .stats{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:1.25rem;margin-bottom:1.75rem}
    .stat{background:var(--bg2);border:1px solid var(--border);border-radius:var(--radius);padding:1.25rem}
    .stat-val{font-size:2rem;font-weight:800}
    .stat-lbl{font-size:.78rem;color:var(--muted);font-weight:500;margin-top:.25rem}
    .stat-icon{font-size:1.5rem;margin-bottom:.25rem}
    /* Sortable */
    .drag-h{cursor:grab;color:var(--muted)}
    .drag-h:active{cursor:grabbing}
    .sortable-ghost{opacity:.4;background:rgba(79,110,247,.1)}
    /* Icon picker */
    .icon-picker-wrap{display:flex;align-items:center;gap:.75rem}
    .icon-preview{width:36px;height:36px;background:var(--bg3);border:1px solid var(--border);border-radius:8px;display:flex;align-items:center;justify-content:center;font-size:1.1rem;color:var(--primary)}
    #icon-modal-overlay{display:none;position:fixed;inset:0;background:rgba(10,12,22,.85);backdrop-filter:blur(12px);z-index:9000;align-items:center;justify-content:center;transition:opacity .2s ease}
    #icon-modal-overlay.open{display:flex}
    #icon-modal{background:var(--bg2);border:1px solid rgba(255,255,255,.1);box-shadow:0 25px 60px rgba(0,0,0,.6), 0 0 80px rgba(79,110,247,.06);border-radius:20px;padding:1.75rem;width:min(720px,95vw);max-height:88vh;display:flex;flex-direction:column;gap:1.25rem;animation:modalScale .3s cubic-bezier(0.34, 1.56, 0.64, 1)}
    @keyframes modalScale {
        from { transform: scale(.92); opacity: 0; }
        to { transform: scale(1); opacity: 1; }
    }
    .icon-search-wrap{position:relative;display:flex;align-items:center}
    .icon-search-wrap sl-icon{position:absolute;left:14px;color:var(--muted);font-size:1.2rem}
    .icon-search-wrap input{padding-left:2.75rem;border-radius:12px;height:46px;font-size:.92rem}
    .icon-search-wrap input:focus{box-shadow:0 0 12px rgba(79,110,247,.25);border-color:var(--primary)}
    .icon-tabs{display:flex;gap:.5rem;overflow-x:auto;padding-bottom:.6rem;border-bottom:1px solid var(--border)}
    .icon-tab{padding:.5rem 1.15rem;border-radius:24px;font-size:.78rem;font-weight:600;background:rgba(255,255,255,.02);color:var(--muted);border:1px solid var(--border);cursor:pointer;white-space:nowrap;transition:all var(--ease);font-family:inherit}
    .icon-tab:hover{background:rgba(255,255,255,.06);color:var(--text)}
    .icon-tab.active{background:linear-gradient(135deg, rgba(79,110,247,.2), rgba(79,110,247,.05));color:#818cf8;border-color:var(--primary);box-shadow:0 4px 12px rgba(79,110,247,.15)}
    .icon-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(56px,1fr));gap:.65rem;overflow-y:auto;max-height:380px;padding-right:.25rem}
    /* Custom scrollbar */
    .icon-grid::-webkit-scrollbar{width:6px}
    .icon-grid::-webkit-scrollbar-track{background:transparent}
    .icon-grid::-webkit-scrollbar-thumb{background:rgba(255,255,255,.12);border-radius:10px}
    .icon-grid::-webkit-scrollbar-thumb:hover{background:rgba(255,255,255,.24)}
    .icon-grid button{width:56px;height:56px;background:var(--bg3);border:1px solid var(--border);border-radius:12px;cursor:pointer;color:var(--text);font-size:1.35rem;display:flex;align-items:center;justify-content:center;transition:all .18s cubic-bezier(0.175, 0.885, 0.32, 1.275)}
    .icon-grid button:hover{background:linear-gradient(135deg, var(--primary), #6366f1);border-color:var(--primary);color:#fff;transform:scale(1.15) rotate(3deg);box-shadow:0 6px 18px rgba(79,110,247,.35)}
    @media(max-width:900px){
        .sb{transform:translateX(-100%)}
        .sb.open{transform:translateX(0)}
        .main{margin-left:0}
        .af-row{grid-template-columns:1fr}
    }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
{% if is_granted('IS_IMPERSONATOR') %}
<div class="imp-bar">Impersonando <strong>{{ app.user.username }}</strong> <a href="{{ path('admin_dash') }}?_switch_user=_exit">Sair</a></div>
{% endif %}

<aside class="sb" id="sidebar">
    <a href="{{ path('admin_dash') }}" class="sb-logo">
        <div class="logo-dot">W</div> WAB Sites
    </a>
    <nav class="nav" aria-label="Admin">
        <div class="nav-sec">Conteúdo</div>
        <a href="{{ path('admin_dash') }}" class="nl {% if app.request.pathInfo == '/admin' %}on{% endif %}">
            <sl-icon name="grid-fill"></sl-icon> Dashboard
        </a>
        <a href="{{ path('admin_category_index') }}" class="nl {% if '/admin/category' in app.request.pathInfo %}on{% endif %}">
            <sl-icon name="tag-fill"></sl-icon> Categorias
        </a>
        <a href="{{ path('admin_page_index') }}" class="nl {% if '/admin/page' in app.request.pathInfo %}on{% endif %}">
            <sl-icon name="file-text-fill"></sl-icon> Páginas
        </a>
        <div class="nav-sec">Comunicação</div>
        <a href="{{ path('admin_contact_index') }}" class="nl {% if '/admin/contact' in app.request.pathInfo %}on{% endif %}">
            <sl-icon name="envelope-fill"></sl-icon> Contato
        </a>
        <a href="{{ path('admin_newsletter_index') }}" class="nl {% if '/admin/newsletter' in app.request.pathInfo %}on{% endif %}">
            <sl-icon name="send-fill"></sl-icon> Newsletter
        </a>
        {% if app.user and app.user.workGroup == 0 %}
        <div class="nav-sec">Gestão</div>
        <a href="{{ path('admin_editor_index') }}" class="nl {% if '/admin/editors' in app.request.pathInfo %}on{% endif %}">
            <sl-icon name="people-fill"></sl-icon> Editores
        </a>
        {% if is_granted('ROLE_SUPER_ADMIN') %}
        <a href="{{ path('admin_settings') }}" class="nl {% if '/admin/settings' in app.request.pathInfo %}on{% endif %}">
            <sl-icon name="gear-fill"></sl-icon> Configurações
        </a>
        {% endif %}
        {% endif %}
        {% if is_granted('ROLE_SUPER_ADMIN') %}
        <div class="nav-sec">Super Admin</div>
        <a href="{{ path('superadmin_dash') }}" class="nl"><sl-icon name="shield-fill"></sl-icon> Painel Global</a>
        {% endif %}
        {% if is_granted('IS_IMPERSONATOR') %}
        <div class="nav-sec" style="color:#f59e0b">Impersonação</div>
        <a href="{{ path('admin_dash') }}?_switch_user=_exit"
           class="nl"
           style="color:#f59e0b;font-weight:700;background:rgba(245,158,11,.08);border-left:3px solid #f59e0b">
            <sl-icon name="person-fill-x"></sl-icon> Sair da impersonação
        </a>
        {% endif %}
    </nav>
    <div class="sb-user">
        <div class="av">{{ app.user ? app.user.name|first|upper : '?' }}</div>
        <div>
            <div class="av-name">{{ app.user ? app.user.name : '' }}</div>
            <div class="av-role">{% if app.user %}{{ app.user.workGroup == 0 ? 'Admin' : 'Editor' }}{% endif %}</div>
        </div>
        <a href="{{ path('app_logout') }}" class="av-logout" title="Sair"><sl-icon name="box-arrow-right" style="font-size:1.1rem"></sl-icon></a>
    </div>
</aside>

<div class="main">
    <header class="tb">
        <div style="display:flex;align-items:center;gap:1rem">
            <button onclick="document.getElementById('sidebar').classList.toggle('open')" style="display:none;background:none;border:none;cursor:pointer;color:var(--text)" id="mob-btn" aria-label="Menu">
                <sl-icon name="list" style="font-size:1.4rem"></sl-icon>
            </button>
            <span class="tb-title">{% block page_title %}Admin{% endblock %}</span>
        </div>
        <a href="{{ path('pub_home') }}" target="_blank" class="btn btn-g btn-sm"><sl-icon name="box-arrow-up-right"></sl-icon> Ver site</a>
    </header>

    <div class="cnt">
        {% for msg in app.flashes('success') %}
            <div class="al al-ok"><sl-icon name="check-circle-fill"></sl-icon> {{ msg }}</div>
        {% endfor %}
        {% for msg in app.flashes('warning') %}
            <div class="al al-warn"><sl-icon name="exclamation-triangle-fill"></sl-icon> {{ msg }}</div>
        {% endfor %}
        {% for msg in app.flashes('error') %}
            <div class="al al-err"><sl-icon name="x-circle-fill"></sl-icon> {{ msg }}</div>
        {% endfor %}
        {% for msg in app.flashes('danger') %}
            <div class="al al-err"><sl-icon name="x-circle-fill"></sl-icon> {{ msg }}</div>
        {% endfor %}
        {% block body %}{% endblock %}
    </div>
</div>

{# ── Icon Picker Modal ── #}
<div id="icon-modal-overlay">
    <div id="icon-modal">
        <div style="display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--border);padding-bottom:.75rem">
            <div style="display:flex;align-items:center;gap:.6rem">
                <sl-icon name="star-fill" style="color:var(--primary);font-size:1.2rem;vertical-align:middle;filter:drop-shadow(0 0 8px rgba(79,110,247,0.4))"></sl-icon>
                <span style="font-weight:800;font-size:1.15rem;letter-spacing:-.01em">Escolher Ícone Font Awesome</span>
            </div>
            <button onclick="closeIconPicker()" style="background:none;border:none;cursor:pointer;color:var(--muted);font-size:1.4rem;display:flex;align-items:center;transition:color var(--ease)" onmouseover="this.style.color='var(--danger)'" onmouseout="this.style.color='var(--muted)'">✕</button>
        </div>
        <div class="icon-search-wrap">
            <sl-icon name="search"></sl-icon>
            <input type="text" id="icon-search" class="ai" placeholder="Pesquisar mais de 1.200 ícones... ex: home, school, facebook" oninput="filterIcons(this.value)">
        </div>
        <div class="icon-tabs" id="icon-tabs-container">
            <button class="icon-tab active" onclick="switchIconTab('all', this)">Todos</button>
            <button class="icon-tab" onclick="switchIconTab('edu', this)">Educação & Cursos</button>
            <button class="icon-tab" onclick="switchIconTab('biz', this)">Negócios & Web</button>
            <button class="icon-tab" onclick="switchIconTab('brands', this)">Marcas & Redes</button>
            <button class="icon-tab" onclick="switchIconTab('media', this)">Interface & Mídia</button>
            <button class="icon-tab" onclick="switchIconTab('health', this)">Saúde & Esporte</button>
            <button class="icon-tab" onclick="switchIconTab('arrows', this)">Setas & Navegação</button>
        </div>
        <div class="icon-grid" id="icon-grid-container"></div>
        
        <!-- Live Preview Pane -->
        <div id="icon-preview-pane" style="display:flex;align-items:center;gap:1.25rem;padding:0.85rem 1.15rem;background:rgba(255,255,255,0.03);border:1px solid var(--border);border-radius:14px;margin-top:0.25rem;min-height:68px;transition:all var(--ease)">
            <div id="icon-preview-pane-icon" style="font-size:2rem;width:48px;height:48px;background:rgba(79,110,247,0.1);border:1px solid rgba(79,110,247,0.25);border-radius:10px;display:flex;align-items:center;justify-content:center;color:var(--primary);transition:all .18s ease">
                <i class="fa-solid fa-face-smile"></i>
            </div>
            <div style="display:flex;flex-direction:column;gap:0.2rem">
                <span id="icon-preview-pane-name" style="font-size:0.9rem;font-weight:700;font-family:monospace;color:var(--text);letter-spacing:0.02em">fa-solid fa-face-smile</span>
                <span id="icon-preview-pane-desc" style="font-size:0.76rem;color:var(--muted)">Passe o mouse sobre qualquer ícone da lista para ver os detalhes em tempo real</span>
            </div>
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded',()=>{
    if(window.innerWidth<=900){
        document.getElementById('mob-btn').style.display='flex';
    }
    loadIconsFromStylesheets();
    
    // Configura o ouvinte de rolagem infinita (Infinite Scroll)
    const grid = document.getElementById('icon-grid-container');
    if (grid) {
        grid.addEventListener('scroll', function() {
            if (this.scrollTop + this.clientHeight >= this.scrollHeight - 40) {
                appendMoreIcons();
            }
        });
    }
});

// ── Icon Picker ────────────────────────────────────────────────
let FA_ICONS = [];
let _pickerTarget = null;
let _pickerPreview = null;
let _currentTab = 'all';
let _currentList = [];
let _renderLimit = 180;
let _renderedCount = 0;

function getIconPrefix(iconName) {
    const brands = [
        'instagram', 'facebook', 'facebook-f', 'youtube', 'linkedin', 'linkedin-in', 'whatsapp', 'twitter', 'tiktok', 'github',
        'google', 'apple', 'microsoft', 'android', 'amazon', 'wordpress', 'paypal', 'stripe',
        'spotify', 'slack', 'discord', 'figma', 'sass', 'html5', 'css3', 'js', 'react', 'vue',
        'angular', 'node', 'python', 'php', 'docker', 'git', 'aws', 'digital-ocean', 'cloudflare',
        'stack-overflow', 'medium', 'vimeo', 'tumblr', 'pinterest', 'snapchat', 'reddit', 'quora',
        'telegram', 'skype', 'zoom', 'trello', 'behance', 'dribbble', 'flickr', 'deviantart',
        'steam', 'twitch', 'xbox', 'playstation', 'nintendo-switch', 'google-play', 'app-store',
        'line', 'weibo', 'weixin', 'vk', 'odnoklassniki', 'mailchimp', 'hubspot', 'salesforce',
        'shopify', 'magento', 'joomla', 'drupal', 'ghost', 'medium-m', 'kickstarter', 'patreon',
        'envato', 'wix', 'squarespace', 'behance-square', 'twitter-square', 'facebook-square', 'google-plus',
        'tumblr-square', 'pinterest-square', 'youtube-square', 'git-square', 'github-square',
        'whatsapp-square', 'linkedin-square', 'skype-business', 'cc-visa', 'cc-mastercard',
        'cc-discover', 'cc-amex', 'cc-paypal', 'cc-stripe', 'cc-amazon-pay', 'cc-apple-pay',
        'google-wallet', 'btc', 'ethereum', 'bitcoin'
    ];
    if (brands.includes(iconName) || brands.some(b => iconName.startsWith(b + '-'))) {
        return 'fa-brands';
    }
    return 'fa-solid';
}

function loadIconsFromStylesheets() {
    const iconSet = new Set();
    
    // Massive built-in static list of over 600 highly popular Font Awesome 6 icons
    const staticList = [
        // Essentials & General
        'house', 'star', 'heart', 'user', 'users', 'user-group', 'user-plus', 'user-minus', 'user-check', 'user-xmark',
        'building', 'landmark', 'school', 'graduation-cap', 'book', 'book-open', 'book-reader', 'chalkboard',
        'chalkboard-user', 'university', 'award', 'trophy', 'medal', 'certificate', 'briefcase', 'briefcase-medical',
        'suitcase', 'folder', 'folder-open', 'folder-plus', 'folder-minus', 'folder-closed', 'file', 'file-lines',
        'file-pdf', 'file-word', 'file-excel', 'file-powerpoint', 'file-csv', 'file-code', 'file-zipper', 'file-image',
        'file-audio', 'file-video', 'file-signature', 'file-contract', 'file-invoice', 'file-invoice-dollar',
        'clipboard', 'clipboard-list', 'clipboard-check', 'clipboard-question', 'copy', 'paste', 'floppy-disk',
        'pen', 'pencil', 'pen-nib', 'pen-fancy', 'pen-to-square', 'brush', 'palette', 'highlighter', 'paint-roller',
        'eraser', 'search', 'magnifying-glass', 'magnifying-glass-plus', 'magnifying-glass-minus', 'filter',
        'funnel-dollar', 'sort', 'sort-up', 'sort-down', 'bars', 'bars-staggered', 'bars-progress', 'list',
        'list-ol', 'list-ul', 'table', 'table-columns', 'table-cells', 'table-cells-large', 'ellipsis', 'ellipsis-vertical',
        'check', 'check-double', 'circle-check', 'check-to-slot', 'times', 'xmark', 'circle-xmark', 'ban',
        'info', 'info-circle', 'circle-info', 'question', 'question-circle', 'circle-question', 'exclamation',
        'exclamation-circle', 'circle-exclamation', 'triangle-exclamation', 'shield', 'shield-halved', 'shield-heart',
        'lock', 'lock-open', 'key', 'key-skeleton', 'gear', 'gears', 'cog', 'cogs', 'wrench', 'hammer',
        'screwdriver-wrench', 'screwdriver', 'tools', 'trash', 'trash-can', 'trash-arrow-up', 'clock',
        'clock-rotate-left', 'calendar', 'calendar-days', 'calendar-check', 'calendar-xmark', 'calendar-plus',
        'calendar-minus', 'bell', 'bell-slash', 'eye', 'eye-slash', 'comment', 'comments', 'comment-dots',
        'comment-medical', 'comment-slash', 'envelope', 'envelope-open', 'envelope-circle-check', 'phone',
        'phone-volume', 'phone-slash', 'mobile', 'mobile-screen', 'tablet', 'tablet-screen-button', 'laptop',
        'desktop', 'server', 'database', 'cloud', 'cloud-arrow-up', 'cloud-arrow-down', 'wifi', 'signal',
        'globe', 'compass', 'map', 'map-pin', 'location-dot', 'location-arrow', 'route', 'road', 'traffic-light',
        'gas-pump', 'charging-station', 'plug', 'battery-full', 'battery-three-quarters', 'battery-half',
        'battery-quarter', 'battery-empty', 'bolt', 'bolt-lightning', 'fire', 'fire-flame-curved', 'leaf',
        'tree', 'seedling', 'plant-wilt', 'sun', 'moon', 'cloud-sun', 'cloud-moon', 'wind', 'umbrella',
        'snowflake', 'water', 'droplet', 'glass-water', 'mug-hot', 'coffee', 'utensils', 'spoon', 'knife',
        'burger', 'pizza-slice', 'apple-whole', 'cake-candles', 'gift', 'shop', 'shopping-bag', 'shopping-cart',
        'bag-shopping', 'cart-shopping', 'tag', 'tags', 'ticket', 'ticket-simple', 'credit-card', 'wallet',
        'coins', 'dollar-sign', 'euro-sign', 'sterling-sign', 'percent', 'percentage', 'receipt', 'invoice',
        'car', 'bus', 'plane', 'ship', 'bicycle', 'motorcycle', 'truck', 'truck-medical', 'train', 'subway',
        'helicopter', 'anchor', 'flag', 'compass-drafting', 'ruler', 'tape', 'cube', 'cubes', 'shapes',
        'arrow-up', 'arrow-down', 'arrow-left', 'arrow-right', 'arrow-up-long', 'arrow-down-long',
        'arrow-left-long', 'arrow-right-long', 'arrows-rotate', 'rotate', 'recycle', 'hourglass',
        'hourglass-half', 'stopwatch', 'gauge', 'calculator', 'plus', 'minus', 'divide', 'equals',
        
        // Education & Cursos
        'graduation-cap', 'school', 'university', 'book', 'book-open', 'book-reader', 'chalkboard',
        'chalkboard-user', 'user-graduate', 'award', 'trophy', 'medal', 'certificate', 'microscope',
        'flask', 'flask-vial', 'dna', 'brain', 'lightbulb', 'atom', 'globe-americas', 'globe-europe',
        'globe-asia', 'map-location', 'compass-drafting', 'pencil', 'pen-nib', 'ruler-combined',
        'calculator', 'shapes', 'palette', 'brush', 'music', 'laptop-code', 'terminal', 'code',
        'puzzle-piece', 'gamepad', 'child', 'children', 'people-group', 'person-chalkboard',
        'book-bookmark', 'bookmark', 'paperclip', 'magnet', 'hourglass-start', 'infinity',
        'masks-theater', 'book-atlas', 'language', 'earth-americas', 'face-smile',
        
        // Negócios & Web
        'briefcase', 'building', 'store', 'shop', 'cart-shopping', 'bag-shopping', 'credit-card',
        'wallet', 'coins', 'dollar-sign', 'euro-sign', 'cent-sign', 'money-bill', 'money-bill-wave',
        'money-bills', 'piggy-bank', 'bank', 'chart-line', 'chart-bar', 'chart-pie', 'chart-column',
        'chart-area', 'gauge', 'tachometer-alt', 'handshake', 'handshake-simple', 'handshake-angle',
        'people-arrows', 'user-tie', 'users-gear', 'address-book', 'address-card', 'signature',
        'stamp', 'scale-balanced', 'gavel', 'calculator', 'file-invoice', 'file-invoice-dollar',
        'receipt', 'percent', 'percentage', 'tags', 'tag', 'box', 'boxes-stacked', 'warehouse',
        'truck', 'dolly', 'industry', 'globe', 'earth-americas', 'earth-europe', 'earth-asia',
        'passport', 'shield-halved', 'key', 'lock', 'vault', 'briefcase-medical',
        
        // Interface & Mídia
        'image', 'images', 'photo-film', 'camera', 'camera-retro', 'video', 'video-slash', 'film',
        'play', 'pause', 'stop', 'backward', 'forward', 'forward-step', 'backward-step',
        'volume-high', 'volume-low', 'volume-mute', 'volume-xmark', 'microphone', 'microphone-lines',
        'headphones', 'music', 'tv', 'radio', 'podcast', 'rss', 'newspaper', 'bullhorn',
        'share-nodes', 'link', 'link-slash', 'arrow-up-right-from-square', 'circle-play', 'circle-pause',
        'circle-stop', 'sliders', 'toggle-on', 'toggle-off', 'sliders-up', 'window-maximize',
        'window-restore', 'window-close', 'xmark', 'check', 'plus', 'minus', 'chevron-up',
        'chevron-down', 'chevron-left', 'chevron-right', 'caret-up', 'caret-down', 'caret-left',
        'caret-right', 'arrow-rotate-left', 'arrow-rotate-right', 'gear', 'gears', 'trash-can',
        'pen-to-square', 'magnifying-glass', 'filter', 'list', 'bars', 'ellipsis', 'clock',
        'calendar-days',
        
        // Saúde & Esporte
        'heart', 'heart-pulse', 'heartbeat', 'stethoscope', 'hospital', 'hospital-user',
        'user-doctor', 'user-nurse', 'user-injured', 'kit-medical', 'briefcase-medical',
        'pills', 'prescription-bottle', 'syringe', 'wheelchair', 'truck-medical', 'virus',
        'virus-slash', 'microbe', 'lungs', 'brain', 'dna', 'spa', 'dumbbell', 'running',
        'person-running', 'person-hiking', 'person-swimming', 'person-biking', 'person-walking',
        'bicycle', 'apple-whole', 'carrot', 'lemon', 'leaf', 'seedling', 'droplet', 'fire-flame-curved',
        'shield-heart', 'hand-holding-medical', 'weight-scale', 'flask-vial', 'child',
        'football', 'basketball', 'baseball', 'soccer-ball', 'volleyball', 'golf-ball', 'bowling-ball',
        'table-tennis-paddle-ball', 'medal', 'trophy', 'award',
        
        // Setas & Navegação
        'arrow-up', 'arrow-down', 'arrow-left', 'arrow-right', 'arrow-up-long', 'arrow-down-long',
        'arrow-left-long', 'arrow-right-long', 'arrow-up-right', 'arrow-up-left', 'arrow-down-right',
        'arrow-down-left', 'arrows-up-down', 'arrows-left-right', 'arrows-rotate', 'rotate',
        'repeat', 'arrows-spin', 'circle-arrow-up', 'circle-arrow-down', 'circle-arrow-left',
        'circle-arrow-right', 'chevron-up', 'chevron-down', 'chevron-left', 'chevron-right',
        'caret-up', 'caret-down', 'caret-left', 'caret-right', 'forward', 'backward', 'forward-step',
        'backward-step', 'arrow-right-to-bracket', 'arrow-right-from-bracket', 'sign-in-alt', 'sign-out-alt',
        'reply', 'share', 'location-arrow', 'compass', 'map-pin', 'location-dot', 'route', 'road',
        'map-location-dot', 'map-location', 'paper-plane',
        
        // Marcas & Redes
        'instagram', 'facebook', 'facebook-f', 'youtube', 'linkedin', 'linkedin-in', 'whatsapp',
        'twitter', 'tiktok', 'github', 'google', 'apple', 'microsoft', 'android', 'amazon',
        'wordpress', 'paypal', 'stripe', 'spotify', 'slack', 'discord', 'figma', 'sass',
        'html5', 'css3', 'js', 'react', 'vue', 'angular', 'node', 'python', 'php', 'docker',
        'git', 'aws', 'digital-ocean', 'cloudflare', 'stack-overflow', 'medium', 'vimeo',
        'tumblr', 'pinterest', 'snapchat', 'reddit', 'quora', 'telegram', 'skype', 'zoom',
        'trello', 'behance', 'dribbble', 'flickr', 'deviantart', 'steam', 'twitch', 'xbox',
        'playstation', 'nintendo-switch', 'google-play', 'app-store'
    ];
    
    // Add all static list elements to seed the set
    staticList.forEach(name => iconSet.add(name));

    try {
        for (let sheet of document.styleSheets) {
            try {
                if (sheet.href && (sheet.href.includes('font-awesome') || sheet.href.includes('all.min.css'))) {
                    const rules = sheet.cssRules || sheet.rules;
                    if (rules) {
                        for (let rule of rules) {
                            if (rule.selectorText) {
                                const matches = rule.selectorText.match(/\.fa-([a-z0-9-]+)::before/g);
                                if (matches) {
                                    for (let match of matches) {
                                        const iconName = match.replace(/^\.fa-/, '').replace(/::before/, '');
                                        if (!['lg', '2xs', 'xs', 'sm', 'xl', '2xl', '3xl', '4xl', '5xl', '6xl', '7xl', '8xl', '9xl', '10xl', '2x', '3x', '4x', '5x', '6x', '7x', '8x', '9x', '10x', 'fw', 'ul', 'li', 'border', 'pull-left', 'pull-right', 'spin', 'pulse', 'rotate-90', 'rotate-180', 'rotate-270', 'flip-horizontal', 'flip-vertical', 'flip-both', 'stack', 'stack-1x', 'stack-2x', 'inverse', 'solid', 'regular', 'light', 'thin', 'duotone', 'brands'].includes(iconName)) {
                                            iconSet.add(iconName);
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            } catch (e) {
                // Cross-origin access could be blocked, ignore
            }
        }
    } catch (globalErr) {
        console.warn("Error loading Font Awesome dynamically from stylesheets:", globalErr);
    }

    FA_ICONS = Array.from(iconSet).map(name => {
        const prefix = getIconPrefix(name);
        return `${prefix} fa-${name}`;
    }).sort();
}

function openIconPicker(inputId, previewId) {
    _pickerTarget = document.getElementById(inputId);
    _pickerPreview = document.getElementById(previewId);
    
    // Refresh/load icons again on modal open (makes sure dynamic stylesheet is processed once it has loaded)
    loadIconsFromStylesheets();
    
    document.getElementById('icon-search').value = '';
    
    // Restaura a aba ativa para "Todos" ao abrir
    const tabs = document.querySelectorAll('.icon-tab');
    tabs.forEach(t => t.classList.remove('active'));
    if (tabs[0]) tabs[0].classList.add('active');
    _currentTab = 'all';
    
    renderIcons(FA_ICONS, true);
    document.getElementById('icon-modal-overlay').classList.add('open');
    document.getElementById('icon-search').focus();
    
    // Set default preview in bottom pane
    if (_pickerTarget && _pickerTarget.value) {
        previewIcon(_pickerTarget.value);
    } else {
        resetPanePreview();
    }
}

function closeIconPicker() {
    document.getElementById('icon-modal-overlay').classList.remove('open');
}

function switchIconTab(category, btn) {
    const tabs = document.querySelectorAll('.icon-tab');
    tabs.forEach(t => t.classList.remove('active'));
    btn.classList.add('active');
    
    _currentTab = category;
    document.getElementById('icon-search').value = '';
    
    const list = getCategorizedIcons(category);
    renderIcons(list, true);
}

function getCategorizedIcons(category) {
    if (category === 'all') return FA_ICONS;
    
    return FA_ICONS.filter(ic => {
        const parts = ic.split(' ');
        const prefix = parts[0];
        const name = parts[1].replace('fa-', '');
        
        if (category === 'brands') {
            return prefix === 'fa-brands';
        }
        
        if (category === 'edu') {
            const keywords = ['graduation', 'book', 'school', 'teacher', 'university', 'college', 'pencil', 'pen', 'brain', 'microscope', 'flask', 'award', 'trophy', 'medal', 'calculator', 'ruler', 'certificate', 'shapes', 'palette', 'dna', 'atlas', 'language'];
            return keywords.some(k => name.includes(k));
        }
        
        if (category === 'biz') {
            const keywords = ['briefcase', 'building', 'store', 'shop', 'shopping', 'credit', 'wallet', 'piggy', 'bank', 'chart', 'tachometer', 'handshake', 'globe', 'server', 'database', 'cloud', 'wifi', 'shield', 'key', 'lock', 'envelope', 'phone', 'bell', 'dollar', 'euro', 'coin', 'receipt', 'invoice', 'vault', 'stamp', 'signature'];
            return keywords.some(k => name.includes(k));
        }
        
        if (category === 'media') {
            const keywords = ['image', 'photo', 'video', 'play', 'music', 'microphone', 'headphones', 'camera', 'film', 'gear', 'tool', 'wrench', 'hammer', 'trash', 'edit', 'check', 'times', 'info', 'question', 'exclamation', 'search', 'filter', 'sort', 'list', 'bars', 'ellipsis', 'clock', 'calendar'];
            return keywords.some(k => name.includes(k));
        }
        
        if (category === 'health') {
            const keywords = ['heart', 'stethoscope', 'hospital', 'doctor', 'nurse', 'injured', 'kit', 'pills', 'syringe', 'wheelchair', 'virus', 'microbe', 'lungs', 'spa', 'dumbbell', 'running', 'hiking', 'swimming', 'biking', 'walking', 'bicycle', 'carrot', 'lemon', 'sport', 'soccer', 'football', 'basketball', 'baseball', 'tennis', 'trophy', 'medal', 'volleyball', 'golf', 'bowling', 'child'];
            return keywords.some(k => name.includes(k));
        }
        
        if (category === 'arrows') {
            const keywords = ['arrow', 'chevron', 'caret', 'exchange', 'rotate', 'sync', 'repeat', 'spin', 'location', 'compass', 'map-pin', 'direction', 'route', 'road', 'sign', 'plane', 'paper-plane'];
            return keywords.some(k => name.includes(k));
        }
        
        return false;
    });
}

function filterIcons(q) {
    const term = q.toLowerCase().trim().replace(/^fa-(solid|brands|regular)\s+fa-/, '').replace(/fa-/,'');
    const baseList = getCategorizedIcons(_currentTab);
    
    if (!term) {
        renderIcons(baseList, true);
        return;
    }
    
    const filtered = baseList.filter(ic => ic.toLowerCase().includes(term));
    renderIcons(filtered, true);
}

function renderIcons(list, resetScroll = true) {
    _currentList = list;
    const grid = document.getElementById('icon-grid-container');
    if (!grid) return;
    
    if (resetScroll) {
        grid.scrollTop = 0;
        _renderedCount = 0;
        grid.innerHTML = '';
    }
    
    if (list.length === 0) {
        grid.innerHTML = '<div style="grid-column:1/-1;text-align:center;padding:3rem;color:var(--muted)">Nenhum ícone encontrado.</div>';
        return;
    }
    
    appendMoreIcons();
}

function appendMoreIcons() {
    const grid = document.getElementById('icon-grid-container');
    if (!grid) return;
    
    const start = _renderedCount;
    const end = Math.min(start + _renderLimit, _currentList.length);
    if (start >= _currentList.length) return;
    
    const nextBatch = _currentList.slice(start, end);
    const html = nextBatch.map(ic =>
        `<button type="button" onclick="selectIcon('${ic}')" onmouseenter="previewIcon('${ic}')" title="${ic}"><i class="${ic}"></i></button>`
    ).join('');
    
    const indicator = document.getElementById('scroll-load-indicator');
    if (indicator) indicator.remove();
    
    const tempDiv = document.createElement('div');
    tempDiv.innerHTML = html;
    while (tempDiv.firstChild) {
        grid.appendChild(tempDiv.firstChild);
    }
    
    _renderedCount = end;
    
    if (_renderedCount < _currentList.length) {
        const sentinel = document.createElement('div');
        sentinel.id = 'scroll-load-indicator';
        sentinel.style.gridColumn = '1 / -1';
        sentinel.style.textAlign = 'center';
        sentinel.style.padding = '1rem 0';
        sentinel.style.fontSize = '0.78rem';
        sentinel.style.color = 'var(--muted)';
        sentinel.innerText = `Role para carregar mais... (${_renderedCount} de ${_currentList.length})`;
        grid.appendChild(sentinel);
    }
}

function selectIcon(cls) {
    if (_pickerTarget) _pickerTarget.value = cls;
    if (_pickerPreview) _pickerPreview.innerHTML = `<i class="${cls}"></i>`;
    closeIconPicker();
}

function previewIcon(cls) {
    const paneIcon = document.getElementById('icon-preview-pane-icon');
    const paneName = document.getElementById('icon-preview-pane-name');
    const paneDesc = document.getElementById('icon-preview-pane-desc');
    if (!paneIcon || !paneName || !paneDesc) return;
    
    paneIcon.innerHTML = `<i class="${cls}"></i>`;
    paneName.innerText = cls;
    
    const parts = cls.split(' ');
    const type = parts[0] === 'fa-brands' ? 'Marca / Logotipo' : 'Estilo Sólido';
    const name = parts[1].replace('fa-', '');
    paneDesc.innerText = `Ícone "${name}" • Tipo: ${type}`;
    
    // Dynamic scale/glow animation on hover
    paneIcon.style.transform = 'scale(1.1)';
    paneIcon.style.borderColor = 'rgba(79, 110, 247, 0.5)';
    paneIcon.style.boxShadow = '0 0 12px rgba(79, 110, 247, 0.25)';
}

function resetPanePreview() {
    const paneIcon = document.getElementById('icon-preview-pane-icon');
    const paneName = document.getElementById('icon-preview-pane-name');
    const paneDesc = document.getElementById('icon-preview-pane-desc');
    if (!paneIcon || !paneName || !paneDesc) return;
    
    paneIcon.innerHTML = `<i class="fa-solid fa-face-smile"></i>`;
    paneName.innerText = 'fa-solid fa-face-smile';
    paneDesc.innerText = 'Passe o mouse sobre qualquer ícone da lista para ver os detalhes em tempo real';
    
    paneIcon.style.transform = 'scale(1)';
    paneIcon.style.borderColor = 'rgba(79, 110, 247, 0.25)';
    paneIcon.style.boxShadow = 'none';
}

document.getElementById('icon-modal-overlay').addEventListener('click', function(e){
    if(e.target === this) closeIconPicker();
});
</script>
{% block javascripts %}{% endblock %}
</body>
</html>
