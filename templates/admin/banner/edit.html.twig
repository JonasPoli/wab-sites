{% extends 'admin/wab_base.html.twig' %}
{% import 'admin/banner/_form.html.twig' as f %}
{% block title %}Editar Banner{% endblock %}{% block page_title %}Editar Banner{% endblock %}
{% block body %}
<div class="ph"><h1>{{ banner.title }}</h1><a href="{{ path('admin_banner_index') }}" class="btn btn-g"><sl-icon name="arrow-left"></sl-icon> Voltar</a></div>
<div class="card">{{ f.banner_form(banner, path('admin_banner_edit', {id: banner.id})) }}</div>
{% endblock %}
